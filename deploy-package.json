{
  "name": "deploy-vercel",
  "version": "1.0.0",
  "description": "Deploy Data Cleaning App to Vercel",
  "scripts": {
    "deploy": "vercel --prod",
    "dev": "vercel dev", 
    "build": "cd frontend && npm run build",
    "setup": "npm install && cd frontend && npm install",
    "update-apis": "python update_api_urls.py",
    "prepare-deploy": "npm run update-apis && npm run build"
  }
}